name: smoke
description: Local no-API run for verifying the re-read memory loop.
data: examples/sample_augmented.jsonl
model:
  hidden_size: 32
  max_length: 96
training:
  epochs: 5
  learning_rate: 0.001
  seed: 7
